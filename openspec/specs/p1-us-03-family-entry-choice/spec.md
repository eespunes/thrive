## Purpose
Let newly authenticated users choose whether to create a family or join an existing one.

## Requirements
### Requirement: Family entry decision screen
The app SHALL display both create-family and join-family options on first family onboarding.

#### Scenario: Choice screen displayed
- **WHEN** an authenticated user has no active family workspace
- **THEN** the app shows actions to create a new family or join with invitation code
